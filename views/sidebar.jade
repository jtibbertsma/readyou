// Settings Tab
.sidebar-tab(ng-click="tabs.toggleTab('settings')", ng-class='{active: tabs.settings}')
  p Editor Settings
.tab-content(ng-show='tabs.settings', ng-controller='EditorSettingsCtrl')
  .sidebar-row
    .sidebar-col
      legend Line wrap
      .radio
        label(for='lwr1')
          input(
            type='radio',
            ng-model='opts.wrap',
            id='lwr1',
            name='lwr',
            ng-value='true',
            ng-checked='opts.wrap'
          )
          | On
      .radio
        label(for='lwr2')
          input(
            type='radio',
            ng-model='opts.wrap',
            id='lwr2',
            name='lwr',
            ng-value='false',
            ng-checked='!opts.wrap'
          )
          | not On
    .sidebar-col
      legend Tab Size
      select.side-select(ng-model='opts.tabSize', ng-options='size for size in sizes')
  .sidebar-row
    .sidebar-col
      legend Soft tabs
      .radio
        label(for='sft1')
          input(
            type='radio',
            ng-model='opts.useSoftTabs',
            id='sft1',
            name='sft',
            ng-value='true',
            ng-checked='opts.useSoftTabs'
          )
          | On
      .radio
        label(for='sft2')
          input(
            type='radio',
            ng-model='opts.useSoftTabs',
            id='sft2',
            name='sft',
            ng-value='false',
            ng-checked='!opts.useSoftTabs'
          )
          | not On